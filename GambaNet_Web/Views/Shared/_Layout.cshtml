@using Microsoft.AspNetCore.Mvc.TagHelpers
<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - GambaNet</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/GambaNet_Web.styles.css" asp-append-version="true" />
    <!-- @await RenderSectionAsync("Styles", required: false) -->
    </head>
<body class="bg-light">
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-dark bg-dark border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">Gamba<span style="color:#56a4e3;">Net</span></a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="" asp-action="">Hry</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Privacy">Soukromí</a>
                        </li>
                        @if (User.Identity.IsAuthenticated == true && (User.IsInRole(nameof(Roles.Admin)) || User.IsInRole(nameof(Roles.Default))))
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-area="" asp-controller="" asp-action="">Uživatelské centrum</a>
                            </li>
                        }
                        @if (User.Identity.IsAuthenticated == true && (User.IsInRole(nameof(Roles.Admin))))
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-area="Admin" asp-controller="Games" asp-action="Select">Úprava Her</a>
                            </li>
                        }
                    </ul>
                    
                    
                @{
                        @if (User.Identity is { IsAuthenticated: false })
                        {
                            <span style="padding-right:16px;">
                                <a class="nav-link" asp-area="Security" asp-controller="Account" asp-action="Login">Login</a>
                            </span>
                            <span>
                                <a class="nav-link" asp-area="Security" asp-controller="Account" asp-action="Register">Register</a>
                            </span>
                        }
                        else
                        {
                            <span style="padding-right:16px;">
                                <a class="nav-link">@User.Identity.Name</a>
                                <a class="dropdown-item" asp-area="Security" asp-controller="Account" asp-action="Logout">Logout</a>
                            </span>
                        }
                    }
                    
                </div>
            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted bg-dark">
        <div class="container text-light">
            &copy;2024 Lukáš Urban & Jakub Kramný.&nbsp;<a asp-area="" asp-controller="Home" asp-action="Privacy" class="text-danger">Prohlášení o ochraně osobních údajů</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
