@page "/info"
@using GambaNet.Infrastructure.Identity
@using Microsoft.AspNetCore.Identity
@using GambaNet_Web.Application.Implementation
@using GambaNet_Web.Application.Abstraction
@using GambaNet_Web.Domain.Entities
@inject UserManager<User> UserManager
@inject SignInManager<User> SignInManager
@inject IBalanceService BalanceService

@{
    var user = await UserManager.GetUserAsync(User);
    var isAuthenticated = User.Identity?.IsAuthenticated ?? false;
    decimal? balance = null;

    if (isAuthenticated && user != null)
    {
        balance = await BalanceService.GetBalanceAsync(user.Id.ToString());
    }
}

<div class="container">
    <h1>Uživatelské informace</h1>
    <br />
    @if (isAuthenticated && user != null)
    {
        <div>
            <p><strong>Uživatelské jméno:</strong> @user.UserName</p>
            <p><strong>Email:</strong> @user.Email</p>
            <p><strong>Registrace:</strong> @user.StartDate?.ToString("yyyy-MM-dd")</p>
            <p><strong>Telefoní číslo:</strong> @user.PhoneNumber</p>
            <p><strong>Jméno:</strong> @user.FirstName</p>
            <p><strong>Příjmení:</strong> @user.LastName</p>
            <p><strong>Peníze:</strong> @balance</p>
            <br />
            <form method="get" asp-controller="Account" asp-action="AddMoney">
                <button type="submit" class="btn btn-primary">Dobít kredit</button>
            </form>
        </div>
    }
    else
    {
        <p>Prosím přihlašte se pro zobrazení údajů.</p>
    }
</div>
